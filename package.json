{
  "name": "workshop-cypress",
  "version": "1.0.0",
  "description": "Testes automatizados frontend e backend utilizando cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.0.0",
    "cypress-plugin-tab": "^1.0.5"
  },
  "dependencies": {
    "cypress-waitfor": "^1.1.0"
  }
}
